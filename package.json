{
  "name": "askelads-warbuddy",
  "version": "0.1.7",
  "private": true,
  "description": "Read-only Torn war action queue and retaliation userscript.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/core.test.mjs",
    "check": "npm run test && npm run build"
  }
}
